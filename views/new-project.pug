extends layout

include mixins/create-project

block content
    .contenido-principal
        h1 #{namePage}

        // pre #{varDump(project)}

        if errors
            each error in errors
                .alerta.error #{error.msg}

        +createProject(project)