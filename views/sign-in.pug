doctype html
html(lang='es')
    head

        include partials/header

    body.login
        .contenedor-formulario
            h1 Inicia sisión en UpTask

            if errors
                if (typeof errors === "object")
                    each error in errors
                        .alerta.error #{error.msg}
                else
                    p.alerta.error #{errors}

            form#formulario.caja.caja-login(method='POST' action='/sign-in')
                .campo
                    loabel(for='email') E-mail:
                    input(type='email' name='email' placeholder='Email' value= email)
                .campo
                    loabel(for='password') Contraseña:
                    input(type='password' name='password' placeholder='Contraseña' value= password)
                .campo.enviar
                    input.boton(type='submit' value='Iniciar Sesión')
                .campo
                    a(href='/create-account') Crea cuenta